@model IEnumerable<BoutiquePortal.Model.Models.Country>

@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    ViewBag.Title = "Country";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between mb-3">
        <h2>Country List</h2>

        <a href="/Admin/Country/AddEdit" class="btn btn-success">
            <i class="fa fa-plus"></i> Add Country
        </a>
    </div>

    <div class="card shadow">
        <div class="card-body table-responsive">

            <table class="table table-bordered table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>#</th>
                        <th>Country Name</th>
                        <th>Code</th>
                        <th>Status</th>
                        <th>Action</th>
                    </tr>
                </thead>

                <tbody>
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="5" class="text-center">No Data Found</td>
                        </tr>
                    }
                    else
                    {
                        int i = 1;
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>@i</td>
                                <td>@item.CountryName</td>
                                <td>@item.CountryCode</td>

                                <td>
                                    <span class="badge bg-@(item.IsActive ? "success" : "danger")">
                                        @(item.IsActive ? "Active" : "Inactive")
                                    </span>
                                </td>

                                <td>
                                    <a href="/Admin/Country/AddEdit/@item.CountryId"
                                       class="btn btn-sm btn-primary">Edit</a>

                                    <form action="/Admin/Country/Delete"
                                          method="post"
                                          class="delete-form d-inline">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="id" value="@item.CountryId" />

                                        <button type="submit" class="btn btn-sm btn-danger">
                                            Delete
                                        </button>
                                    </form>
                                </td>
                            </tr>
                            i++;
                        }
                    }
                </tbody>
            </table>

        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/admin/location.js"></script>
}